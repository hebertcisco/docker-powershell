<script>
  let currentPage = window.location.pathname;
  let currentPageElement = document.getElementById(currentPage);
  if (currentPageElement) {
      currentPageElement.innerHTML = `Index of ${currentPage}`;
  }
</script>